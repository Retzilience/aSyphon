# Ignore everything by default
*

# Allow Git to see directories so later negations work
!*/

# Allow source + common repo files
!*.py
!*.md
!README*
!LICENSE*
!.gitignore

# Allow the update descriptor
!version.upd

# Explicit ignores (mostly redundant with '*', kept for clarity)
dist/
__pycache__/
*.zip
*.png
aSyphon02
# allow png assets for the repo UI/readme/etc
!assets/**/*.png
